import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jacobthornton.evangelize',
  appName: 'Evangelize',
  webDir: 'dist/evangelize/browser'
};

export default config;
